# Rust build output
/target

# Release artifacts are attached to GitHub Releases, not committed
/dist/*.exe
/dist/*.zip

# Personal tooling: traction numbers are the owner's business, not the product's
/scripts/

# Editor / OS cruft
*.pdb
.vs/
.idea/
.DS_Store
Thumbs.db
