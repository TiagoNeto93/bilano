# Statically link the MSVC C runtime so the shipped .exe has no external
# runtime dependency (no VC++ Redistributable needed on the target machine).
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
